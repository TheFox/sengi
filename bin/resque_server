#!/usr/bin/env bash

set -x
set -e
DATE=$(date +"%Y-%m-%d %H:%M:%S %Z")
SCRIPT_BASEDIR=$(dirname $0)
SCRIPT_BASENAME=$(basename $0)


cd "${SCRIPT_BASEDIR}/.."

resque-web -F -L --host 127.0.0.1 --port 8282 -r 127.0.0.1:7000:0
