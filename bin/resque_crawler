#!/usr/bin/env bash

set -e
DATE=$(date +"%Y-%m-%d %H:%M:%S %Z")
SCRIPT_BASEDIR=$(dirname $0)
SCRIPT_BASENAME=$(basename $0)


cd "${SCRIPT_BASEDIR}/.."

#export QUEUE=crawler
#export BACKGROUND=yes
#export PIDFILE=./run/queue_${QUEUE}.pid

QUEUE=crawler INTERVAL=0.1 rake resque:work
